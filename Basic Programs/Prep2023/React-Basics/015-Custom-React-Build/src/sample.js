export const sampleOp=()=>{
    console.log("Sample Op from Sample.js");
}

const sampleIP=()=>{
    console.log("Sample Ip from Sample.js");
}

export default sampleIP;


//export {export- named import and export default- only one file can be imported to any name}

// when we have only one function then standard practice is to make it as default

// directed graph of dependency - webpack is creating - (bundling process)