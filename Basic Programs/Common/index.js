// Get all div elements
const divs = document.querySelector("div");
divs.style.width="300px";
divs.style.height="300px";
divs.style.background="blue";

// Loop through each div element and set the style properties
// for (let i = 0; i < divs.length; i++) {
//     divs[i].style.width = "200px";   
//     divs[i].style.height = "200px";        // Set the width to 200 pixels
//     divs[i].style.background = "#565435"; // Set the background color
// }